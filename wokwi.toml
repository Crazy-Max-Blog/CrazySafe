[wokwi]
version = 1
elf = ".pio/build/mgbot-iotik32b/firmware.elf"
firmware = ".pio/build/mgbot-iotik32b/firmware.bin"

# Forward http://localhost:8180 to port 80 on the simulated ESP32:
[[net.forward]]
from = "localhost:8180"
to = "target:80"